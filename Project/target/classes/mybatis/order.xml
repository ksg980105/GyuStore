<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace = "product.OrderBean">
	
	<insert id="insertOrder">
		insert into ordertable (order_number, name, email, phone, address1, address2, pname, pop_out, point, requestOrder, pimage, productPrice, using_point)
		values(order_seq.nextval, #{name}, #{email}, #{phone}, #{address1}, #{address2}, #{pname}, #{pop_out}, #{point}, #{requestOrder}, #{pimage}, #{productPrice}, #{using_point})
	</insert>
	
	<select id="getAllOrder" resultType="product.model.OrderBean">
		select * from ordertable
		where email = #{email}
		order by order_number desc
	</select>
	
</mapper> 